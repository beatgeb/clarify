<?php
$projects = $db->data('SELECT id, name, screen_count FROM project ORDER BY name ASC');
?>
<h3>Account<a class="btn-logout" href="<?= R ?>auth/?wipe=1">Logout</a></h3>
<div class="usermeta">
    <div class="avatar"><img src="<?= $_SESSION['user']->profile_image_url ?>" width="48" height="48" /></div>
    <div class="info">
        <p class="username">Roger Dudler</p>
        <p>Namics AG</p>
    </div>
</div>
<h3>Projects<a class="btn-add-project" href="javascript:;">+ Add</a></h3>
<div class="add-project">
    <input class="project-name" type="text" placeholder="Project Name" />
    <input class="add-project-submit" type="button" value="Add Project" />
</div>
<ul>
    <? foreach ($projects as $p) { ?>
    <? $notifications = $p['id'] == 5 ? '<span class="notifications">5</span>' : ''; ?>
    <li <?= $p['id'] == $project_id ? 'class="active"': '' ?>>
        <a href="<?= R ?>project/<?= $p['id'] ?>"><?= $p['name'] ?> <span class="count"><?= $p['screen_count'] ?></span><?= $notifications ?></a>
    </li>
    <? } ?>
</ul>
<? if ($project_id > 0) { ?>
<h3><?= $project['name'] ?></h3>
<ul>
    <li><a href="javascript:;">Settings</a></li>
    <li><a href="javascript:;" data-project="<?= $project_id ?>" class="btn-delete-project">Delete Project</a></li>
</ul>
<? } ?>
<div class="beta-notice">This is a very early version of Clarify. Please report issues &amp; bugs to <a href="mailto:roger@clarify.io">roger@clarify.io</a>.</div>